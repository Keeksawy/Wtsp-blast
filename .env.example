COMPANY_NAME=Your Company Name
DEFAULT_AGENT_NAME=Your Name
PORT=3000
# Set a strong password — required to access the dashboard and all API endpoints.
# Change this before running the server for the first time.
DASHBOARD_PASSWORD=changeme
# Paste your CRM webhook URL here — POSTed to when a contact replies SELL or RENT.
CRM_WEBHOOK_URL=
